// This represents the height of the waterfall in seconds
export const WATERFALL_TIME_GAP = 10;
// Measured in seconds. The margin above and below canvas where we still "render".
// Allows overflow clipping (aka if element is long, doesn't disappear sooner than it should).
export const INVISIBLE_GAP = 2;
export const WATERFALL_TOP_PADDING = 30;
export const WATERFALL_KEY_HEIGHT = 10;
// The window of time in seconds given to the user before and after a note finishes where it still counts
export const GAMEPLAY_ERROR_MARGIN = 1;
export const GAMEPLAY_SCORE_BASE = 100;
